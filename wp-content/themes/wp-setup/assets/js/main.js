(function($) {

  $.fn.Plugin = function() {
    var $main = $(this);

    function init(){
      setVars();
      bindEvents();
    }

    function setVars(){
    }

    function bindEvents(){
    }

    init();
  };

})(jQuery);
